import packageMetadata from "../../package.json";

export const APP_NAME = packageMetadata.productName;
export const APP_PACKAGE_NAME = packageMetadata.name;
export const APP_VERSION = packageMetadata.version;
export const APP_AUTHOR = packageMetadata.author;
export const APP_REPOSITORY_URL = packageMetadata.repository.url;
